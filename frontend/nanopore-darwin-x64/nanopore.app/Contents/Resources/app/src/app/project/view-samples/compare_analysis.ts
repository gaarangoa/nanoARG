export class GeneCounts {

    public network: any;

    constructor() {}

    build_table(data: any, reads: number) {
        // the input to this function are the nodes to the network.
        data['nodes'].forEach(element => {
            
        });
    }

}
